import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <section class="fixed right-[2vw] top-[50vh]">
      {/* @todo - implement side navigator make it visible after one scrolls bellow the hero section */}
      {/* <p class="p1">
        Nav
      </p> */}
    </section>
  )
})